package routes

import (
    "github.com/gin-gonic/gin"
    // "github.com/wpcodevo/golang-fiber-mysql/controllers"
)

// SetProductRoutes sets up routes for product-related actions
func SetProductRoutes(router *gin.RouterGroup) {
    // productController := &controllers.ProductController{}

    // Create a new product (POST request)
    // router.POST("/", productController.CreateProduct)

    // // Get a product by ID (GET request)
    // router.GET("/:id", productController.GetProduct)

    // // Update a product by ID (PUT request)
    // router.PUT("/:id", productController.UpdateProduct)

    // // Delete a product by ID (DELETE request)
    // router.DELETE("/:id", productController.DeleteProduct)
}
